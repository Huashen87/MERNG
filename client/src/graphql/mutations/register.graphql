mutation Register(
  $username: String!
  $password: String!
  $email: String!
  $confirmPassword: String!
) {
  register(
    username: $username
    password: $password
    email: $email
    confirmPassword: $confirmPassword
  ) {
    id
    username
    email
    token
    createdAt
  }
}
